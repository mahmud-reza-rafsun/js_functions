/**
 * Objective: write a function to give me the sum of all numbers in an array.
 * step-1: declear a function.
 * setp-2: call check whether the function is called properly.
 * step-3: set a parameters(s).
 * step-4: pass the parameters(s), check whether parameter is passed in a proper format.
 */

function sumOfNumber(numbers){
    for(const number of numbers){
        console.log(nums)
    }
}
const nums = [43, 45, 32, 56, 34, 54, 43];
const sum = sumOfNumber(nums);
console.log('Sum of numbers is', sum);